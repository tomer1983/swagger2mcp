# Frontend Configuration

# Backend API URL (without /api suffix - will be added by the client)
VITE_API_URL=http://localhost:3000

# Environment mode (development, staging, production)
# This is set automatically by Vite based on the build command
# VITE_MODE=development

# Feature flags (optional)
# VITE_ENABLE_ANALYTICS=false
# VITE_ENABLE_DEBUG_MODE=false

# Production settings (override these for production builds)
# VITE_API_URL=https://api.yourdomain.com

# Note: All VITE_* variables are embedded in the client-side bundle
# Do NOT put sensitive secrets here - they will be visible in the browser

# Production Environment Variables Template
# Copy this to .env.production and fill in your actual values
# NEVER commit the actual .env.production file to git!

# WARNING: This file contains sensitive production configuration
# Keep it secure and never share publicly

# ===================================
# Server Configuration
# ===================================
NODE_ENV=production
PORT=3000

# ===================================
# Database Configuration
# ===================================
# PostgreSQL connection string
# For managed services, use the connection string from your provider
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE?options
DATABASE_URL=postgresql://swagger2mcp_user:CHANGE_THIS_PASSWORD@postgres-host:5432/swagger2mcp_prod?schema=public&connection_limit=20

# Database credentials (for docker-compose)
POSTGRES_USER=swagger2mcp_user
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_DB=swagger2mcp_prod

# ===================================
# Redis Configuration
# ===================================
REDIS_HOST=redis-host
REDIS_PORT=6379
# Optional: Add password for Redis in production
# REDIS_PASSWORD=CHANGE_THIS_PASSWORD

# ===================================
# Authentication & Security
# ===================================
# JWT Secret - MUST be changed! Generate with: openssl rand -base64 64
JWT_SECRET=CHANGE_THIS_TO_RANDOM_64_CHAR_STRING_GENERATED_WITH_OPENSSL_RAND_BASE64_64
JWT_EXPIRES_IN=7d

# Microsoft OAuth2 (Optional - only if using Microsoft authentication)
# MICROSOFT_CLIENT_ID=your-microsoft-app-client-id
# MICROSOFT_CLIENT_SECRET=your-microsoft-app-client-secret
# MICROSOFT_CALLBACK_URL=https://yourdomain.com/api/auth/microsoft/callback

# ===================================
# Application Configuration
# ===================================
# Frontend URL (used for OAuth redirects and CORS)
FRONTEND_URL=https://yourdomain.com

# Anonymous Access
# Set to 'false' to require authentication, 'true' to allow anonymous access
ALLOW_ANONYMOUS=false

# ===================================
# CORS Configuration
# ===================================
# Comma-separated list of allowed origins
# In production, specify your actual domains
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ===================================
# Deployment Configuration
# ===================================
# Docker image version tag
VERSION=1.0.0

# ===================================
# Optional: External Services
# ===================================
# Monitoring (if using)
# SENTRY_DSN=your-sentry-dsn
# NEW_RELIC_LICENSE_KEY=your-new-relic-key

# Email service (if implemented)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM=noreply@yourdomain.com

# File storage (if using S3/GCS instead of local)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=us-east-1

# ===================================
# Security Notes
# ===================================
# 1. Generate strong random values for all secrets
# 2. Use different secrets for each environment (dev/staging/prod)
# 3. Rotate secrets regularly (recommended: quarterly)
# 4. Use a secrets manager in production (AWS Secrets Manager, HashiCorp Vault, etc.)
# 5. Never commit this file with real values to version control
# 6. Limit access to this file on production servers (chmod 600)

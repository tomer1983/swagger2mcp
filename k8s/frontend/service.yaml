apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: swagger2mcp
  labels:
    app.kubernetes.io/name: swagger2mcp
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 5173
      targetPort: 5173
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: swagger2mcp
    app.kubernetes.io/component: frontend

{
  "name": "swagger2mcp-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "shadcn": "^3.5.1"
  }
}